package TestJava;

import java.nio.ByteBuffer;

import org.w3c.dom.NodeList;

public interface testInterface {
	int s = 0 ;	
	public static final int s2 = 0;
	//-------------上面的两种成员变量的定义方法是一样的，因为接口中的成员变量只能定义成public static final类型。
	//接口中的方法默认是public abstract ，但是Java8后规定接口中可以有default方法
	
	
}
